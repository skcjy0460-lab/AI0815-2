# -*- coding: utf-8 -*-
import streamlit as st

st.title("📘 사용 가이드")

st.markdown("""
### 이 프로그램은 무엇인가요?
기존에 운영 중인 **원무통계 · 심사평가 · 인력관리 · 재무제표** 4개 시스템에서 각각 만들어진
HTML 보고서를 업로드하면, **경영 맞춤 AI**가 4개 데이터를 **교차 분석**하여 병원장이 바로 볼 수 있는
**한 단계 상위의 'AI 경영 브리핑'**을 자동 생성합니다.

단순히 4개 보고서를 요약하는 것이 아니라, *"원무통계의 환자 감소가 재무제표의 매출 정체와
같은 시기에 나타나는가"*, *"인력관리의 인건비율 상승이 재무제표의 인건비 비율 초과와 맞물리는가"*
와 같이 **개별 보고서만으로는 보이지 않는 인사이트**를 찾아내는 것이 핵심 가치입니다.

---

### 사용 순서
1. **API · 환경 설정**에서 경영 맞춤 AI 연동 키를 등록합니다. (배포 시에는 `secrets.toml` 권장)
2. **새 브리핑 생성**에서 4개 시스템의 HTML 보고서를 업로드합니다. (1~4개, 일부만 있어도 가능)
3. 자동 인식된 보고서 유형이 맞는지 확인하고, 필요 시 직접 수정합니다.
4. 병원명 · 브리핑 기간 · 추가 메모를 입력합니다.
5. **AI 경영 브리핑 생성하기** 버튼을 누르면 약 30초~1분 내 프리미엄 리포트가 생성됩니다.
6. 결과를 미리보기로 확인하고, **HTML로 저장**하거나 **보관함에 저장**합니다.

---

### 모델 자동 폴백은 어떻게 동작하나요?
기본적으로 `gemini-3.6-flash` → `gemini-3.5-flash-lite` 순서로 시도합니다.
1순위 모델의 사용량이 초과되면(429 / RESOURCE_EXHAUSTED) 자동으로 다음 모델로 전환되어
서비스가 끊기지 않습니다. 모델 순서는 **API · 환경 설정** 페이지에서 자유롭게 변경할 수 있습니다.

---

### 보고서 유형이 자동으로 인식되지 않아요
파일명과 본문 키워드를 기준으로 자동 분류하지만, 시스템 버전에 따라 마크업이 달라지면
인식률이 떨어질 수 있습니다. 이 경우 업로드 화면의 **"보고서 유형"** 드롭다운에서 직접
지정해주세요. AI 분석에는 문제가 없습니다 (본문 텍스트 전체를 함께 전달하기 때문입니다).

---

### 데이터는 어디에 저장되나요?
생성된 브리핑은 서버(배포 환경)의 로컬 파일로 저장되며, 업로드된 원본 보고서 자체는
저장하지 않고 분석 즉시 폐기합니다. 민감한 환자 개인정보가 원본 보고서에 포함되지 않도록
사전에 확인해주세요.

---

### 유료 라이선스는 어떻게 발급하나요?
`secrets.toml`의 `LICENSE_KEYS` 목록에 고객사별 키를 등록하면, 해당 키를 입력한
사용자만 앱에 접근할 수 있습니다. 별도 결제/구독 시스템과 연동하려면
`core/auth.py`의 `require_license()` 함수를 확장하면 됩니다.
""")
